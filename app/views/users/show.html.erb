<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

  <p>
    <strong>Roles:</strong>
  	<% @user.role_names.each do |name| %>
  	<%= label_tag role_name(name) ,role_name(name) , :class => 'success label'%>
  	<% end %>
  </p>

<%= link_to 'Edit', edit_user_path(@user) ,:class => 'button tiny radius'%> |
<%= link_to 'Back', users_path ,:class => 'button tiny radius'%>
